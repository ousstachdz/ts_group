<?xml version="1.0" ?>
<odoo>
    <record id="purchase_request_line_tree" model="ir.ui.view">
        <field name="name">purchase.request.line.tree</field>
        <field name="model">purchase.request.line</field>
        <field name="arch" type="xml">
            <list
                name="Purchase Request Lines"
                create="false"
                decoration-muted="cancelled == True"
                decoration-info="request_state in ('draft', 'to_approve')"
            >
                <field name="request_id" />
                <field
                    name="request_state"
                    widget="badge"
                    decoration-success="request_state in ('done', 'approved')"
                    decoration-info="request_state in ('draft', 'to_approve')"
                    decoration-danger="request_state == 'rejected'"
                />
                <field name="requested_by" widget="many2one_avatar_user" />
                <field name="assigned_to" widget="many2one_avatar_user" />
                <field name="date_required" />
                <field name="estimated_cost" widget="monetary" />
                <field name="currency_id" invisible="1" />
                <field name="purchased_qty" />
                <field
                    name="purchase_state"
                    widget="badge"
                    decoration-success="purchase_state == 'done'"
                    decoration-muted="purchase_state == 'draft'"
                    decoration-info="purchase_state in ('sent', 'purchase')"
                    decoration-warning="purchase_state == 'to_approve'"
                    decoration-danger="purchase_state == 'cancelled'"
                />
                <field name="product_id" />
                <field name="name" />
                <field name="product_qty" />
                <field name="product_uom_id" groups="uom.group_uom" />
                <field
                    name="analytic_distribution"
                    widget="analytic_distribution"
                    optional="hide"
                    groups="analytic.group_analytic_accounting"
                    options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}"
                />
                <field name="supplier_id" />
                <field
                    name="company_id"
                    groups="base.group_multi_company"
                    widget="selection"
                />
                <field name="cancelled" invisible="1" />
                <field name="qty_in_progress" invisible="1" />
                <field name="qty_done" invisible="1" />
                <field name="qty_cancelled" invisible="1" />
            </list>
        </field>
    </record>
   
    <record id="purchase_request_line_form" model="ir.ui.view">
        <field name="name">purchase.request.line.form</field>
        <field name="model">purchase.request.line</field>
        <field name="priority" eval="20" />
        <field name="arch" type="xml">
            <form string="Purchase Request Line" create="false" duplicate="false">
                
                <sheet>
                    <h1>
                        <field name="request_id" />
                    </h1>
                    <group name="main">
                        <group name="info">
                            <field
                                name="origin"
                                readonly="not is_editable"
                            />
                            <field
                                name="requested_by"
                                readonly="not is_editable"
                            />
                            <field
                                name="assigned_to"
                                readonly="not is_editable"
                            />
                            <field
                                name="description"
                                readonly="not is_editable"
                            />
                            <field
                                name="date_start"
                                readonly="not is_editable"
                            />
                            <field
                                name="company_id"
                                groups="base.group_multi_company"
                                widget="selection"
                                readonly="not is_editable"
                            />
                            <field name="is_editable" invisible="1" />
                        </group>
                        <group name="settings">
                            <field
                                name="product_id"
                                readonly="not is_editable"
                            />
                            <field
                                name="name"
                                readonly="not is_editable"
                            />
                            <label for="product_qty" />
                            <div>
                                <field
                                    name="product_qty"
                                    class="oe_inline"
                                    readonly="not is_editable"
                                />
                                <field
                                    name="product_uom_id"
                                    groups="uom.group_uom"
                                    class="oe_inline"
                                    readonly="not is_editable"
                                />
                            </div>
                            <field
                                name="analytic_distribution"
                                widget="analytic_distribution"
                                groups="analytic.group_analytic_accounting"
                                options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}"
                            />
                            <field
                                name="date_required"
                                readonly="not is_editable"
                            />
                            <field
                                name="estimated_cost"
                                widget="monetary"
                                readonly="not is_editable"
                            />
                            <field name="currency_id" invisible="1" />
                            <field name="cancelled" />
                        </group>
                    </group>
                    <group name="specifications">
                        <separator string="Specifications" />
                        <newline />
                        <field
                            name="specifications"
                            nolabel="1"
                            readonly="not is_editable"
                        />
                    </group>
                    <group name="quantities" string="Tracking">
                        <group>
                            <field name="pending_qty_to_receive" />
                            <field name="qty_in_progress" />
                            <field name="qty_done" />
                            <field name="qty_cancelled" />
                        </group>
                    </group>
                    <notebook>
                        <page name="purchase_lines" string="Purchase Order Lines">
                            <group>
                                <field name="purchased_qty" />
                                <field name="purchase_state" />
                            </group>
                            <field
                                name="purchase_lines"
                                mode="list"
                                readonly="purchase_state == 'cancel'"
                                domain="[('product_id', '=', product_id)]"
                            />
                        </page>
                        <page name="allocations" string="Allocations">
                            <field name="purchase_request_allocation_ids">
                                <list decoration-muted="purchase_state == 'cancel'">
                                    <field name="purchase_line_id" />
                                    <field name="stock_move_id" />
                                    <field name="product_id" />
                                    <field name="requested_product_uom_qty" />
                                    <field name="allocated_product_qty" />
                                    <field name="open_product_qty" />
                                    <field name="product_uom_id" />
                                    <field name="purchase_state" invisible="1" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter reload_on_attachment="True"/>
            </form>
        </field>
    </record>

    <record id="purchase_request_line_search" model="ir.ui.view">
        <field name="name">purchase.request.line.search</field>
        <field name="model">purchase.request.line</field>
        <field name="arch" type="xml">
            <search string="Search Purchase Request">
                <filter name="purchase_state_none" string="Purchasing not started" domain="[('purchase_state','=', False)]" help="No RFQ or Purchase has been created"/>
                <filter name="purchase_state_draft" string="Requested for Quotation" domain="[('purchase_state','=','draft')]" help="At least a Draft PO has been created"/>
                <filter name="purchase_state_confirmed" string="Purchase Confirmed" domain="[('purchase_state','=','confirmed')]" help="At least a PO has been confirmed"/>
                <filter name="purchase_state_done" string="Purchase Done" domain="[('purchase_state','=','done')]" help="At least a PO has been completed"/>
                <filter name="request_state_draft" string="Draft" domain="[('request_state','=','draft')]" help="Request is to be approved"/>
                <filter name="request_state_to_approve" string="To Approve" domain="[('request_state','=','to_approve')]" help="Request is to be approved"/>
                <filter name="request_state_approved" string="Approved" domain="[('request_state','=','approved')]" help="Request is approved"/>
                <filter name="request_state_rejected" string="Rejected" domain="[('request_state','=','rejected')]" help="Request is rejected"/>
                <separator/>
                <filter name="assigned_to_me" domain="[( 'assigned_to','=', uid)]" help="Assigned to me"/>
                <separator/>
                <filter name="my_requests" domain="[('requested_by','=', uid)]" help="My requests"/>
                <separator/>
                <filter name="uncancelled" domain="[('cancelled','=', False)]" string="Uncancelled"/>
                <separator/>
                <filter name="qty_to_buy" string="Pending Qty to Receive" domain="[('qty_to_buy','=',True)]" />
                <separator/>
                <filter name="all_bought" string="All Bought" domain="[('qty_to_buy','=',False)]"/>
                <separator/>
                <filter name="open" domain="[('qty_in_progress','!=', 0.0)]" string="Open Quantity"/>
                <separator/>
                <filter name="cancel" domain="[('qty_cancelled','>', 0.0)]" string="Cancelled Quantity"/>
                <separator/>
                <field name="request_id" />
                <field name="product_id" />
                <field name="requested_by" />
                <field name="assigned_to" />
                <field name="date_start" />
                <field name="date_required" />
                <field name="analytic_distribution" groups="analytic.group_analytic_accounting"/>
                <field name="cancelled" invisible="1" />
                <group>
                    <filter name="product" string="Product" context="{'group_by' : 'product_id'}"/>
                    <filter name="requested_by" string="Requested by" context="{'group_by':'requested_by'}"/>
                    <filter name="assigned_to" string="Assigned to" context="{'group_by':'assigned_to'}"/>
                    <filter name="request_status" string="Request status" context="{'group_by':'request_state'}"/>
                    <filter name="group_analytic_distribution" string="Analytic Distribution" context="{'group_by':'analytic_distribution'}" groups="analytic.group_analytic_accounting"/>
                    <filter name="purchase_status" string="Purchase Status" context="{'group_by':'purchase_state'}"/>
                    <filter name="request" string="Request" context="{'group_by':'request_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="purchase_request_line_view_action" model="ir.actions.act_window">
        <field name="name">Purchase Request Line</field>
        <field name="res_model">purchase.request.line</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create the first purchase request Line</p>
        </field>
    </record>

    <menuitem
        id="menu_purchase_request_line_act"
        sequence="20"
        parent="menu_purchase_request"
        action="purchase_request_line_view_action"/>
    
    <menuitem
        action="purchase_request_line_view_action"
        id="menu_purchase_request_line"
        parent="purchase.menu_procurement_management"
    />
    
</odoo>
