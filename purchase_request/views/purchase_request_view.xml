<?xml version="1.0" ?>

<odoo>
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <button
                        name="button_draft"
                        invisible=" state not in ('to_approve','approved','rejected','done')"
                        string="Reset"
                        type="object"
                        groups="purchase_request.group_purchase_request_manager"
                    />
                    <button
                        name="button_to_approve"
                        invisible="state != 'draft'"
                        string="Request approval"
                        type="object"
                        class="oe_highlight"
                    />
                    <button
                        name="button_approved"
                        invisible="state != 'to_approve'"
                        string="Approve"
                        type="object"
                        class="oe_highlight"
                        groups="purchase_request.group_purchase_request_manager"
                    />
                    <button
                        name="%(action_purchase_request_line_make_purchase_order)d"
                        invisible="state != 'approved'"
                        string="Create RFQ"
                        type="action"
                    />
                    <button
                        name="button_done"
                        invisible="state != 'approved'"
                        string="Done"
                        type="object"
                        class="oe_highlight"
                        groups="purchase_request.group_purchase_request_manager"
                    />
                    <button
                        name="button_rejected"
                        invisible="state in ('to_approve','approved')"
                        string="Reject"
                        type="object"
                        groups="purchase_request.group_purchase_request_manager"
                    />
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,to_approve,approved,done,rejected"
                        statusbar_colors='{"approved":"blue"}'
                    />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            type="object"
                            name="action_view_purchase_request_line"
                            class="oe_stat_button"
                            invisible="line_count == 0"
                            icon="fa-list"
                        >
                            <field name="line_count" widget="statinfo" string="Lines" />
                        </button>
                        <button
                            type="object"
                            name="action_view_purchase_order"
                            class="oe_stat_button"
                            invisible="purchase_count == 0"
                            icon="fa-shopping-cart"
                            string="Purchase Order"
                        >
                            <field
                                name="purchase_count"
                                widget="statinfo"
                                string="Purchase Orders"
                            />
                        </button>
                        <button
                            type="object"
                            name="action_view_stock_picking"
                            class="oe_stat_button"
                            invisible="move_count == 0"
                            groups="stock.group_stock_manager"
                            icon="fa-truck"
                            string="Picking"
                        >
                            <field
                                name="move_count"
                                widget="statinfo"
                                string="Pickings"
                            />
                        </button>
                    </div>
                    <div class="oe_edit_only">
                        <label for="name" class="oe_inline" />
                    </div>
                    <h1>
                        <field name="is_editable" invisible="1" />
                        <field name="is_name_editable" invisible="1" />
                        <field
                            name="name"
                            class="oe_inline"
                            readonly="not is_name_editable"
                        />
                    </h1>
                    <group>
                        <group>
                            <field
                                name="requested_by"
                                readonly="not is_editable"
                            />
                            <field
                                name="assigned_to"
                                readonly="not is_editable"
                            />
                        </group>
                        <group>
                            <field
                                name="origin"
                                class="oe_inline"
                                readonly="not is_editable"
                            />
                            <field
                                name="description"
                                readonly="not is_editable"
                            />
                            <field
                                name="group_id"
                                readonly="not is_editable"
                            />
                        </group>
                        <group>
                            <field
                                name="date_start"
                                readonly="not is_editable"
                            />
                            <field
                                name="picking_type_id"
                                readonly="not is_editable"
                            />
                            <field
                                name="company_id"
                                groups="base.group_multi_company"
                                widget="selection"
                                readonly="not is_editable"
                            />
                        </group>
                    </group>
                    <notebook>
                        <page string="Products">
                            <field name="line_ids">
                                <list
                                    decoration-muted="cancelled == True"
                                    editable="bottom"
                                >
                                    <field name="product_id" />
                                    <field name="name" />
                                    <field name="product_qty" />
                                    <field
                                        name="product_uom_id"
                                        invisible="1"
                                        groups="!uom.group_uom"
                                    />
                                    <field
                                        name="product_uom_id"
                                        groups="uom.group_uom"
                                    />
                                    <field
                                        name="analytic_distribution"
                                        widget="analytic_distribution"
                                        groups="analytic.group_analytic_accounting"
                                        options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}"
                                    />
                                    <field name="date_required" />
                                    <field name="estimated_cost" widget="monetary" />
                                    <field name="currency_id" invisible="1" />
                                    <field
                                        name="company_id"
                                        groups="base.group_multi_company"
                                        widget="selection"
                                    />
                                    <field name="cancelled" invisible="1" />
                                    <field name="is_editable" invisible="1" />
                                    <field name="purchased_qty" />
                                    <field
                                        name="purchase_state"
                                        widget="badge"
                                        decoration-success="purchase_state == ('done')"
                                        decoration-muted="purchase_state == ('draft')"
                                        decoration-info="purchase_state in ('sent', 'purchase')"
                                        decoration-warning="purchase_state == ('to_approve')"
                                        decoration-danger="purchase_state == 'cancelled'"
                                    />
                                    <button
                                        name="action_show_details"
                                        type="object"
                                        icon="fa-list"
                                        title="Show Details"
                                        width="0.1"
                                        options='{"warn": true}'
                                    />
                                </list>
                            </field>
                            <group class="oe_subtotal_footer oe_right">
                                <field name="currency_id" invisible="1" />
                                <div class="oe_subtotal_footer_separator oe_inline">
                                    <label for="estimated_cost" />
                                </div>
                                <field
                                    name="estimated_cost"
                                    nolabel="1"
                                    class="oe_subtotal_footer_separator"
                                    widget="monetary"
                                    options="{'currency_field': 'currency_id'}"
                                />
                            </group>

                        </page>
                    </notebook>
                </sheet>
                <chatter reload_on_attachment="True"/>
            </form>
        </field>
    </record>

    <record id="view_purchase_request_tree" model="ir.ui.view">
        <field name="name">purchase.request.tree</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list decoration-bf="message_needaction==True"
                    decoration-info="state in ('draft','to_approve')"
                    decoration-muted="state in ('rejected')"
                    name="Purchase Request">
                <field name="message_needaction" invisible="1" />
                <field name="name" />
                <field name="date_start" />
                <field name="requested_by" widget="many2one_avatar_user" />
                <field
                    name="company_id"
                    groups="base.group_multi_company"
                    widget="selection"
                />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field name="origin" />
                <field name="currency_id" invisible="1" />
                <field name="estimated_cost" optional="hide" />
                <field
                    name="state"
                    widget="badge"
                    decoration-success="state in ('done', 'approved')"
                    decoration-muted="state == 'draft'"
                    decoration-warning="state == 'to_approve'"
                    decoration-danger="state == 'rejected'"
                />
            </list>
        </field>
    </record>

    <record id="purchase_request_search_view" model="ir.ui.view">
        <field name="name">purchase.request.search.view</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <search string="Search Purchase Request">
                <filter name="unassigned" string="Unassigned" domain="[('assigned_to','=', False)]" help="Unassigned Request"/>
                <filter name="state_draft" string="Draft" domain="[('state','=','draft')]" help="Request is to be approved"/>
                <filter name="state_to_approve" string="To Approve" domain="[('state','=','to_approve')]" help="Request is to be approved"/>
                <filter name="state_approved" string="Approved" domain="[('state','=','approved')]" help="Request is approved"/>
                <filter name="state_rejected" string="Rejected" domain="[('state','=','rejected')]" help="Request is rejected"/>
                <filter name="state_done" string="Done" domain="[('state','=','done')]" help="Request is done"/>
                <separator />
                <filter name="assigned_to_me" domain="[('assigned_to','=', uid)]" help="Assigned to me"/>
                <separator />
                <filter name="my_requests" domain="[('requested_by','=', uid)]" help="My requests"/>
                <separator />
                <field name="name" string="Purchase Request" />
                <field name="product_id"/>
                <field name="state"/>
                <field name="requested_by" />
                <field name="assigned_to" />
                <field name="picking_type_id" />
                <separator />
                <group>
                    <filter name="requested_by" string="Requested by" icon="fa-user" context="{'group_by':'requested_by'}"/>
                    <filter name="assigned_to" string="Assigned to" icon="fa-user" context="{'group_by':'assigned_to'}"/>
                    <filter name="source" string="Source" icon="fa-caret-square-o-left" context="{'group_by':'origin'}"/>
                    <filter name="start_date" string="Start Date" icon="fa-calendar" context="{'group_by':'date_start'}"/>
                </group>
            </search>
        </field>
    </record>
    
    <record id="purchase_request_view_action" model="ir.actions.act_window">
        <field name="name">Purchase Request</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create the first purchase request</p>
        </field>
    </record>

    <menuitem
        id="menu_purchase_request_pro_mgt"
        parent="purchase.menu_procurement_management"
        action="purchase_request_view_action"
    />
    <menuitem
        id="parent_menu_purchase_request"
        name="Purchase Requests"
        sequence="22"
        web_icon="purchase_request,static/description/icon.png"
        groups="group_purchase_request_user,group_purchase_request_manager"
    />
    <menuitem
        id="menu_purchase_request"
        name="Purchase Requests"
        parent="parent_menu_purchase_request"
        groups="group_purchase_request_user,group_purchase_request_manager"
    />
    <menuitem
        id="menu_purchase_request_act"
        sequence="10"
        parent="menu_purchase_request"
        action="purchase_request_view_action"
    />
</odoo>
